<!DOCTYPE html>  
<html lang="en">  
	<head>
		<meta charset="utf-8">
		<title>Link shortener</title>
		<meta name="description" content="Simpl URL shortening service">
		<meta name="author" content="Peter Frankowski">

		<!-- styles -->
		<link href="css/style.css" rel="stylesheet">
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">

	</head>
	<body ng-app="ls">
		<div class="container" ng-controller="mainCtrl">
			<div class="row">
				<div class="col-sm-12">
					<h1>Link shortener</h1>
					<hr>
				</div>
			</div>
			<div class="row">
				<div class="col-sm-6 input">
					<input type="text" ng-enter ng-model="addr" placeholder="Enter link here..."> <button ng-click="shorten();" class="btn btn-success" ng-disabled="addr.length == 0">Shorten</button>
				</div>
				<div class="col-sm-6 output">
					<div ng-show="new_link.length > 0">
						<b class="text-info"> Your link:</b> <a href="{{new_link}}" target="_blank" class="text-success" id="link_elem">{{new_link}}</a> 
						<button class="btn btn-primary glyphicon glyphicon-copy" ng-clipcopy></button>
						<button class="btn btn-danger glyphicon glyphicon-trash" ng-click="clear()"></button>
					</div>	
					<p class="text-warning" ng-show="new_link.length == 0 && err_msg.length == 0">Your link will be here</p>
					<p class="text-danger" ng-show="err_msg.length > 0">{{err_msg}}</p>
				</div>
			</div>
		</div>
		<script src="js/lib/angular/angular.js" type="text/javascript"></script>
		<script src="http://code.jquery.com/jquery-1.12.0.min.js" type="text/javascript"></script>
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
		<script src="js/app.js" type="text/javascript"></script>
		<script src="js/controllers.js" type="text/javascript"></script>
		<script src="js/directives.js" type="text/javascript"></script>
		<script src="js/services.js" type="text/javascript"></script>
	</body>
</html> 